# 這個檔案「刻意」進版控，跟 .env 不同——裡面沒有秘密。
#
# NEXT_PUBLIC_ 開頭的變數會被編進前端 bundle，任何拿到 App 或打開網頁原始碼的人都看得到，
# 本來就不是秘密。放進版控是為了讓「在哪台機器 build」不影響產出。
#
# 為什麼重要：iOS 的 App 要在 Mac 上用 Xcode 出包，但正式環境和 .env 都在 GCP 主機上。
# 如果這個值只存在 .env（未進版控），在 Mac 上 clone 下來建置時它會是空字串，前端就會用
# 相對路徑打 API——而 App 的前端是從 binary 載入的（origin capacitor://localhost），
# 相對路徑會打到 App 自己身上，結果是每支 API 都失敗，且不會有任何建置錯誤提示。
#
# 換後端網址時改這裡，然後重跑 npm run sync:ios。
NEXT_PUBLIC_API_BASE_URL="https://zeno.zequo.net"
